const BASE_URL: string = 'https://projectpal.up.railway.app'

const registerEndpoint: string = `${BASE_URL}/register`

const loginEndpoint: string = `${BASE_URL}/login`


export {
    registerEndpoint,
    loginEndpoint
}